name: PR · Quality Gate

on:
  pull_request:
    branches: [main, master, develop]
    types: [opened, synchronize, reopened]

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_NOLOGO: true
  RESULTS_DIR: TestResults

jobs:
  # ─────────────────────────────────────────────
  # JOB 1 – Build & Format
  # ─────────────────────────────────────────────
  build-and-format:
    name: Build & Format Check
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore dependencies
        run: dotnet restore AgendaPro.sln

      - name: Build (Release)
        run: dotnet build AgendaPro.sln --no-restore --configuration Release -warnaserror

      - name: Verify formatting
        run: dotnet format AgendaPro.sln --verify-no-changes --no-restore
        # Se este passo falhar, execute `dotnet format AgendaPro.sln` localmente
        # e inclua as mudanças de formatação no PR.

  # ─────────────────────────────────────────────
  # JOB 2 – Tests & Coverage
  # ─────────────────────────────────────────────
  test-and-coverage:
    name: Tests & Coverage Gate
    runs-on: ubuntu-latest
    needs: build-and-format
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore AgendaPro.sln

      # Build debug para cobertura precisa (símbolos de debug habilitados)
      - name: Build (Debug)
        run: dotnet build AgendaPro.sln --no-restore --configuration Debug

      # ── Testes unitários ──────────────────────
      - name: Run AgendaPro.UnitTests
        run: |
          dotnet test tests/AgendaPro.UnitTests/AgendaPro.UnitTests.csproj \
            --no-build \
            --configuration Debug \
            --collect:"XPlat Code Coverage" \
            --results-directory "${{ env.RESULTS_DIR }}/UnitTests" \
            --settings coverlet.runsettings \
            --logger "trx;LogFileName=unit-tests.trx" \
            --verbosity normal

      # ── Testes de integração ──────────────────
      - name: Run AgendaPro.Tests
        run: |
          dotnet test tests/AgendaPro.Tests/AgendaPro.Tests.csproj \
            --no-build \
            --configuration Debug \
            --collect:"XPlat Code Coverage" \
            --results-directory "${{ env.RESULTS_DIR }}/IntegrationTests" \
            --settings coverlet.runsettings \
            --logger "trx;LogFileName=integration-tests.trx" \
            --verbosity normal

      # ── Instalar ReportGenerator ──────────────
      - name: Install ReportGenerator
        run: dotnet tool install --global dotnet-reportgenerator-globaltool

      # ── Consolidar relatório de cobertura ─────
      - name: Generate combined coverage report
        run: |
          reportgenerator \
            -reports:"${{ env.RESULTS_DIR }}/**/coverage.cobertura.xml" \
            -targetdir:CoverageReport \
            -reporttypes:"Cobertura;HtmlInline;MarkdownSummaryGithub" \
            -assemblyfilters:"+AgendaPro.*;-AgendaPro.Tests*;-AgendaPro.UnitTests*" \
            -title:"AgendaPro Coverage" \
            -tag:"PR-${{ github.event.pull_request.number }}"

      # ── Validar cobertura mínima ──────────────
      - name: Validate coverage thresholds
        run: |
          python3 - <<'EOF'
          import xml.etree.ElementTree as ET
          import sys

          tree = ET.parse('CoverageReport/Cobertura.xml')
          root = tree.getroot()

          failures = []

          # ── 1. Cobertura global ≥ 80 % ─────────────────────────────────────
          global_rate = float(root.attrib.get('line-rate', 0))
          global_pct  = round(global_rate * 100, 2)
          print(f'Global line coverage : {global_pct} %')
          if global_pct < 80:
              failures.append(f'Global coverage {global_pct}% < 80% (obrigatório)')

          # ── 2. Domain e Application ≥ 85 % ─────────────────────────────────
          layer_lines = {}
          layer_hits  = {}

          for pkg in root.iter('package'):
              pkg_name = pkg.attrib.get('name', '')
              for clazz in pkg.iter('class'):
                  for layer in ('AgendaPro.Domain', 'AgendaPro.Application'):
                      if pkg_name.startswith(layer) or clazz.attrib.get('filename','').replace('\\','/').find(layer.replace('.','/')) != -1:
                          for line in clazz.iter('line'):
                              layer_lines[layer] = layer_lines.get(layer, 0) + 1
                              if int(line.attrib.get('hits', 0)) > 0:
                                  layer_hits[layer] = layer_hits.get(layer, 0) + 1

          for layer in ('AgendaPro.Domain', 'AgendaPro.Application'):
              total = layer_lines.get(layer, 0)
              if total == 0:
                  print(f'{layer}: sem linhas instrumentadas (pulando validação)')
                  continue
              hits = layer_hits.get(layer, 0)
              pct  = round(hits / total * 100, 2)
              print(f'{layer} coverage: {pct} %')
              if pct < 85:
                  failures.append(f'{layer} coverage {pct}% < 85% (obrigatório)')

          # ── Resultado final ─────────────────────────────────────────────────
          if failures:
              for f in failures:
                  print(f'::error::{f}')
              sys.exit(1)
          else:
              print('Todos os thresholds de cobertura foram atendidos.')
          EOF

      # ── Publicar resumo no PR ─────────────────
      - name: Post coverage summary to PR
        if: always()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          recreate: true
          path: CoverageReport/SummaryGithub.md

      # ── Upload artefatos ──────────────────────
      - name: Upload coverage HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-pr-${{ github.event.pull_request.number }}
          path: CoverageReport/
          retention-days: 14

      - name: Upload test results (TRX)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-pr-${{ github.event.pull_request.number }}
          path: ${{ env.RESULTS_DIR }}/**/*.trx
          retention-days: 7

  # ─────────────────────────────────────────────
  # JOB 3 – Architecture Tests (opcional, ative quando o projeto NetArchTest for criado)
  # ─────────────────────────────────────────────
  # architecture-tests:
  #   name: Architecture Tests
  #   runs-on: ubuntu-latest
  #   needs: build-and-format
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         global-json-file: global.json
  #     - run: dotnet restore AgendaPro.sln
  #     - run: dotnet build AgendaPro.sln --no-restore
  #     - run: dotnet test tests/AgendaPro.ArchTests/AgendaPro.ArchTests.csproj --no-build

  # ─────────────────────────────────────────────
  # JOB 4 – SonarCloud (descomente e configure SONAR_TOKEN + SONAR_PROJECT_KEY)
  # ─────────────────────────────────────────────
  # sonarcloud:
  #   name: SonarCloud Analysis
  #   runs-on: ubuntu-latest
  #   needs: build-and-format
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         global-json-file: global.json
  #     - name: SonarCloud Scan
  #       uses: SonarSource/sonarcloud-github-action@master
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  #       with:
  #         args: >
  #           -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
  #           -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
  #           -Dsonar.cs.opencover.reportsPaths=TestResults/**/coverage.opencover.xml
  #           -Dsonar.coverage.exclusions=**/Migrations/**,**/Program.cs,**/*Dto*.cs
  #           -Dsonar.newCode.referenceBranch=main
